import 'package:flutter/material.dart';

/// Used with [GoRouter] so venue surfaces can refresh when a pushed route is popped.
final RouteObserver<PageRoute<void>> playScoutRouteObserver = RouteObserver<PageRoute<void>>();
